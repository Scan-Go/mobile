import { SafeAreaView } from 'react-native-safe-area-context';
import { styled } from 'tamagui';

export const SafeAreaV = styled(SafeAreaView, {
  name: 'SafeAreaV',
  flex: 1,
  backgroundColor: '$background'
});
